-- The original query has a syntax error:
-- SELECT * FROM my_table WHERE my_column = 'some value;

-- Here's the corrected query with proper quote closing:
SELECT * FROM my_table WHERE my_column = 'some value';